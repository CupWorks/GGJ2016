<UserControl
    x:Class="Game.Client.UWA.HistoryView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">
    <Grid Name="RootGrid" Background="Transparent" HorizontalAlignment="Right">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="0" />
        </Grid.ColumnDefinitions>
        <Button Grid.Column="0" x:Name="VisibleButton" VerticalAlignment="Top" Content="show" Click="OnClick" />
        <ListBox Grid.Column="1" Background="Gray" />
    </Grid>
</UserControl>
